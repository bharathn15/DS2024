blinker==1.8.2
click==8.1.7
colorama==0.4.6
Flask==2.0.2   # Assuming Flask 2.x series for stability (adjust as per your needs)
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
packaging==24.1
waitress==2.1.0   # Adjusted to a stable version compatible with Flask 2.x
Werkzeug==3.0.3   # Assuming compatibility with Flask 2.x
